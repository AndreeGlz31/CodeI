<!DOCTYPE html>
<html lang="en">
<div class="container">
 <?php
	foreach
	<h3>Flights from: <?=$vuelo['PaisOrigen']?> to <?=$vuelo['PaisDestino']?></h3>

    <table class="table">
        <thead>
        <tr>
            <th>Choose</th>
            <th>Flight #</th>
            <th>Airline</th>
		<?php
			foreach($vuelos as $vuelo) : ?>
			<th>Departs: <?=$vuelo['PaisOrigen']?></th>
		<?php endforeach; ?>

            <?php
    			foreach($vuelos as $vuelo) : ?>
			<th>Arrives: <?=$vuelo['PaisDestino']?></th>
		<?php endforeach; ?>
            <th>Price</th>
        </tr>
        </thead>
        <tbody>
  <?php
	foreach ($vuelos as $vuelo) : ?>

       <tr>
	<form name="VA43" method="post" action="compra.php">
           <td><input type="submit" class="btn btn-small" value="Choose This Flight"></td>
            <td><?= $vuelo['IDVuelo'] ?></td>
            <td><?= $vuelo['Aerolinea'] ?></td>
            <td><?= $vuelo['HoraDespeje'] ?></td>
            <td><?= $vuelo['HoraAterrizaje'] ?></td>
            <td><?= $vuelo['Precio'] ?></td>
        </tr>
	</form>
        <?php endforeach; ?>
    </tbody>
    </table>
    <input type="hidden" name="fromPort" value="Paris"/>
    <input type="hidden" name="toPort" value="Buenos Aires"/>
    </div>
</div>
